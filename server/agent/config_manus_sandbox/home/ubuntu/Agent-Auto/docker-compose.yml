version: "3.8"

services:
  app:
    build: .
    ports:
      - "${PORT:-5000}:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - APP_DOMAIN=${APP_DOMAIN:-}
      - CORS_ORIGINS=${CORS_ORIGINS:-}
    restart: unless-stopped
