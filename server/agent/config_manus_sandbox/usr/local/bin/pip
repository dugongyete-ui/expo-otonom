#!/bin/bash
# Reset Python environment variables to avoid conflicts
export PYTHONHOME=
export PYTHONPATH=
# Commands that modify the environment need --system flag
case "$1" in
    install|uninstall|sync|list)
        exec uv pip "$1" --system "${@:2}"
        ;;
    *)
        exec uv pip "$@"
        ;;
esac
